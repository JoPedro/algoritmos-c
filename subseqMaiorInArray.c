#include <stdio.h>

int subseqMaiorInArray(int[], int);

int main() {
    int n;
    scanf("%d", &n);
    int arr[n];
    for (int i = 0; i < n; ++i)
        scanf("%d", &arr[i]);
}

int subseqMaiorInArray(int arr[], int n) {
    for (int i = 0; i < sub; ++i)
}